/* eslint-disable no-unused-vars */
import React, {useState} from 'react';
import {
  View,
  Text,
  TouchableOpacity,
  Image,
  FlatList,
  StyleSheet,
} from 'react-native';

const UserInfor = ({navigation}) => {
  return (
    <View style={styles.container}>
      <Text>UserInfor</Text>
    </View>
  );
};

export default UserInfor;

const styles = StyleSheet.create({
  container: {flex: 1},
});
